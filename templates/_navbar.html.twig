<nav class="navbar navbar-dark bg-primary">
  <a class="navbar-brand" href="/">KILLER STORIES</a>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link" href="{{ path('killer_index') }}">Découvre les tueurs <span class="sr-only"></span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('country_index') }}">Cherche par pays</a>
            </li>
        </ul>
    </div>
</nav>
{% if app.user %}
    {{ app.user.username }}
    <a href="{{ path('app_logout') }}"> Se déconnecter</a>
{% else %}
    <a href="{{ path('app_login') }}"> Se connecter</a>
{% endif %}