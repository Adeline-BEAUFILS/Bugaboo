{% extends 'base.html.twig' %}
{% block title %}{{ killer.name }}{% endblock %}

{% block body %}

<div class="media">
  <img class="align-self-start mr-3" src="{{killer.image}}" alt="{{ killer.name }} image">
  <div class="media-body">
    <h1 class="mt-0">{{ killer.firstname }} {{ killer.name }}</h1>
    <h3>{{ killer.aka }}</h3>
    <h2>{{ killer.summary }}</h2>
  </div>
</div>

<a href="{{ path('killer_index') }}">Retour à la liste</a>
{# <a href="{{ path('killer_edit', {'id': killer.id}) }}">edit</a> #}

{% endblock %}